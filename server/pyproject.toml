[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Justin-K-Ellis", email = "jlke45@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "black>=26.5.1",
    "fastapi[standard]>=0.141.1",
    "pytest>=9.1.1",
]

[project.scripts]
server = "app:main"

[tool.fastapi]
entrypoint = "server.main:app"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
